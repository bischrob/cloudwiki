# Implementation TODO

Primary backlog source: `../TODO.md`.

## Immediate Next Actions
1. Create Nextcloud app skeleton (`appinfo`, `lib`, `src`, `templates`, `tests`).
2. Add CI for lint/tests/package validation.
3. Implement open/save APIs with conflict detection.
4. Build CodeMirror raw editor shell.
5. Implement wikilink parser + suggestion endpoint.

## Milestones
- M1: App scaffold and routes running.
- M2: File open/save stable with conflicts handled.
- M3: Wikilinks + backlinks usable end-to-end.

See: [[MVP_Scope]], [[Resumption_Checklist]].
